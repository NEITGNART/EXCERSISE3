#include "finfichero.h"

int main(void) {
	ios_base::sync_with_stdio(false); cin.tie(0);
	unsigned long long n = 1032522;
	string nameTextFile = "input.txt";
	finfichero(n, nameTextFile);

	return 0;
}